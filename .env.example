# ─── Database & Server (shared) ─────────────────
# Both Postgres and the Express server read these.
# docker-compose.yml maps DB_* → POSTGRES_* for the database container.
DB_USER=postgres
DB_PASSWORD=change_me_in_production
DB_NAME=scorecast

# ─── Server-only ────────────────────────────────
# DB_HOST is set in docker-compose.yml (db service name)
# PORT is set in docker-compose.yml (3001)

# ─── CORS ───────────────────────────────────────
# Comma-separated list of allowed origins (set to your domain in production)
# CORS_ORIGIN=https://yourdomain.com
