# ─── Database ───────────────────────────────────
POSTGRES_USER=postgres
POSTGRES_PASSWORD=change_me_in_production
POSTGRES_DB=scorecast

# ─── Server (passed as DB_* env vars) ──────────
# These are derived from the values above in docker-compose.yml.
# Only override here if you need different values for the server.
# DB_USER, DB_PASSWORD, DB_HOST, DB_PORT, DB_NAME

# ─── CORS ───────────────────────────────────────
# Comma-separated list of allowed origins (set to your domain in production)
# CORS_ORIGIN=https://yourdomain.com
